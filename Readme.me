# URL Shortener with Logging

This is a full-stack URL shortener app built with React and Node.js.  
You can shorten links, track clicks, and view stats in a clean UI.  
It includes a custom logging system that sends logs to an external server.  
Frontend is built using React and Material UI.  
Backend handles short link generation, redirection, and analytics.  
To run locally, start both frontend and backend servers.
Need mongodb server running locally.
